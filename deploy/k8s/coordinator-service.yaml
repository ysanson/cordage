# ClusterIP is enough: `cordage query` reaches this via
# `kubectl port-forward svc/cordage-coordinator 9000:9000`.
apiVersion: v1
kind: Service
metadata:
  name: cordage-coordinator
spec:
  selector:
    app: cordage-coordinator
  ports:
  - port: 9000
    targetPort: 9000
